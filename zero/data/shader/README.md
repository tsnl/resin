# Compiled Shaders

This directory contains SPIR-V compiled shaders (.spv files) that are generated from Slang source files in the `shader/` directory.

Run `make shaders` to compile shaders using `slangc`.

## Files
- `*.vert.spv` - Compiled vertex shaders
- `*.frag.spv` - Compiled fragment shaders
