# Resin Bitmap Font Cooker

Pre-generates bitmap font atlases for the Resin SDK.

Iterates over font definitions and generates glyph atlases for all combinations of font sizes, weights, and scales, storing them as cooked atlas files for fast loading at runtime.
