Help me expand RendererAtlas and RendererImage to implement a virtual texture solution. When the user creates a `RendererImage`, we allocate space on a large, multi-page atlas dynamically. Atlases are entirely hidden from end users. 

Restrict your changes to `renderer.py` to start. We'll update callers shortly.

Let's rewrite `RendererAtlas`. 
- `RendererAtlas` should contain a list of 4096x4096 GpuImage instances called "pages", all of which are texture-binding usage, linear colorspace, float32 dtype. Initially, there's just a single element in the list. The RendererAtlas can add pages dynamically.
- `RendererAtlas` should expose a method, `RendererAtlas.allocate_image()` that takes in an image size in pixels and returns a unique image ID, which is an index into an `image_allocations` ndarray. This is a record array with dtype `rect_tile_x: u8, rect_tile_y: u8, rect_w_tiles: u8, rect_h_tiles: u8, page_index: u32` where rect dimensions are in units of 16x16 pixel tiles. This array should be allocated against a growing po2 capacity with a separate allocation_count int being stored. If requested image size isn't a tile multiple, raise a LogicError.
- Each page should store a "cursor" and "current_scaline_height" to insert in scanline fashion. We will first pack greedily, repacking (compaction) before we allocate new pages.
  - `for page in reversed(page_list): try_allocate_in_page(page)`: try to allocate the requested rect on a page, starting with the last page and going through each existing page until the first.
  - `compact_pages(page_list, allocations); try_allocate_in_page(page_list[-1])`: 
    - compaction: copy all pages' content back to the CPU from the GPU. Sort all existing allocations in decreasing order of area. Then, re-insert the rectangles (DO NOT disturb indices or the existing allocation array, instead create a new one) from largest rect to smallest rect. Do not insert any new pages. After all the reallocations are complete, we will have the old images on CPU, the array of old allocations, and the array of new allocations, such that the array of old allocations and new allocations are parallel. We can then (a) use a large per-page staging buffer to copy each destination page's allocations from the source CPU data to the destination GPU image, and then (b) update the old allocations table in-place to point to the new allocation rectangle, which may be a totally different page and rectangle.
    - finally, try allocating in the last page. If this allocation fails, then insert a new page and insert the new rect there.

This is fairly complicated, so write a new class in a new Python file named `PageRectAllocator` in `page_rect_allocator.py`. Write a dedicated test for this in `tests/page_rect_allocator_test.py`. This allocator maintains the internal NumPy arrays of allocations but NOT the pages themselves. When a rectangle is allocated, the allocator will return both the allocation index and whether compaction/page addition occurred. If compaction/page addition occurred, the caller (i.e. RendererAtlas) knows to copy all pages to CPU and write back to the GPU at the correct locations. Otherwise, the caller (i.e. RendererAtlas) knows to just copy the allocated rectangle data. The allocator should also expose an API to query each rect's pixel coordinates and page index given an allocation index.

First implement the page allocator. Test it with a small set of tests, no need to be exhaustive. Just one test case for the simple allocation, one test case for compaction with one page, one test case for compaction with two pages, and one test case for compaction with one page where a new page is required. The page allocator should take the tile size, page size, etc as parameters and should check them in the constructor, throwing LogicError if incoherent (e.g. if tile size is 1 and page size is >256x256, then we do not have enough precision to cover the whole page, so raise). Add a test case for this behavior too.

Next, rewrite the RendererAtlas to use the PageRectAllocator to manage the pages. The PageRectAllocator decides how many pages there are and how they are used. After each `PageRectAllocator.alloc_rect()` call, the RendererAtlas should "sync" changes with the PageRectAllocator, ensuring the number of actual pages matches what the allocator has, performing copies if needed, etc.

Next, expose a new descriptor set layout and descriptor set for the `RendererAtlas` that we can use in `renderer_quad_pipeline` but also other pipelines in the future. The bindings should include a texture array (pages), a uniform buffer (size 64K) containing the allocations, tightly packed, and another uniform containing the tile size, page size in pixels. We should also write a function in `renderer_quad_pipeline.slang` that lets callers sample an image using an image ID and a UV coordinate using SampleLevel. Use a combined imagearray-sampler.

Next, integrate this common descriptor set into the quad pipeline. Note that we no longer need to batch our quads: the shader can now access the required image in "bindless" style. We also don't need a special white atlas, but instead a special white image, which should be 1x1 tile in size. This simplifies things a lot. Delete `RendererQuadBatch` and just use the existing pipeline. Update the `QUAD_DTYPE` to specify the `renderer_image_id` instead of the `atlas_id`. Remove the `src_uv` field. Reduce the size of each quad in an array from 32B to just 24B since we can get rid of the reserved entries and simplify `dst_px` to just `dst_rect` which is `xywh` ie half the size.

Run `make tests` to test stuff out. Ensure you run `source ~/VulkanSDK/1.4.328.1/setup-env.sh`