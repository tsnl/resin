[project]
name = "resin-gen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["egglog>=12.0.0", "ninja>=1.13.0", "pytest>=9.0.2"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.build-backend]
data = { data = "src/resin_gen/bundled_data" }
module-root = ""
