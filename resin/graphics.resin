//! Value types shared by graphics shader inputs and outputs.

export { Position, Color, Vertex };

struct Position {
	x: f32,
	y: f32,
	z: f32,
	w: f32,
}
struct Color {
	r: f32,
	g: f32,
	b: f32,
	a: f32,
}
struct Vertex {
	position: Position,
	color: Color,
}
