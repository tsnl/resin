export { Host };

import { "status.resin" };

struct Host {
	// Host.alloc(count, initial) returns initialized shared element storage.
	@host_allocation_error
	def allocation_error() -> OutOfMemory = {
		OutOfMemory {}
	};
};
