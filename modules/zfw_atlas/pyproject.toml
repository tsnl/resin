[project]
name = "zfw_atlas"
version = "0.1.0"
description = "Read and write texture atlases."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.10,<0.10.0", "uv"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "zfw_atlas"
module-root = ""
