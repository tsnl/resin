export { main };

import { "$/string.resin", "$/stdio.resin" };

fn add(x: int, y: int) -> int {
	x + y
}

fn apply(f: (int, int) -> int, x: int, y: int) -> int {
	f(x, y)
}

fn inc(n: int) -> int {
	apply(add, n, 1)
}

fn main() -> () {
	print(fmt("inc(41) = {0}\n", (inc(41),)));
}
