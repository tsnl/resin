export { main };

def add(x: int, y: int) -> int = {
	x + y
};

def apply(f: (int, int) -> int, args: (int, int)) -> int = {
	f(args)
};

def inc(n: int) -> int = {
	apply(add, (n, 1))
};

def main() -> () = {
	print(fmt("inc(41) = {0}\n", (inc(41),)));
};
