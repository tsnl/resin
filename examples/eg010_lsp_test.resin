export { main };

import { "$/string.resin" };

struct Point2f { x: float32; y: float32; }

fn main()  {
	let mut p = Point2f { x = 42.5, y = 0.012 };
	let mut a = p.x;
	print(fmt("Hello, {0}, {1}\n", (p.x, p.y)));
}
