export { main };

import { "$/string.resin" };

fn fibonacci(n: int) -> _  {
	if (n <= 1) {
		n
	} else {
		fibonacci(n - 1) + fibonacci(n - 2)
	}
}

fn main()  {
	let mut n: _;
	let mut pointer: Ptr<_>;
	n = fibonacci(10);
	pointer = &n;
	print(fmt("fibonacci(10) = {0}\n", (pointer.*,)));
}
