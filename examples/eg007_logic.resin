// Short-circuit boolean ops, comparisons, and a record literal.
clamp = (n: int, lo: int, hi: int) => if ((n < lo) || (n > hi)) { lo } else { n };
both = (a: int, b: int) => { (a != 0) && (b != 0) };
pair = (a: int, b: int) => { a = a, b = b };
xs = [1, 2, 3];
