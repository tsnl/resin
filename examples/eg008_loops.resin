export { main };

import { "$/string.resin", "$/stdio.resin" };

fn main() {
	let mut n = 1;
	let mut sum = 0;
	while (n <= 10) {
		sum = sum + n;
		n = n + 1;
	};
	let text = fmt("sum(1..10) = {0}\n", (sum,));
	print(text);
}
