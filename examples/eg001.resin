export { main };

import { "$/string.resin" };

fn fibonacci(n: int) -> int {
	if (n <= 1) {
		n
	} else {
		let mut f0 = fibonacci(n - 1);
		let mut f1 = fibonacci(n - 2);
		f0 + f1
	}
}

fn main() -> () {
	print(fmt("fibonacci(10) = {0}\n", (fibonacci(10),)));
}
