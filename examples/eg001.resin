export { main };

def fibonacci(n: int) -> int = {
	if (n <= 1) {
		n
	} else {
		var f0 = fibonacci(n - 1);
		var f1 = fibonacci(n - 2);
		f0 + f1
	}
};

def main() -> () = {
	print(
		"fibonacci(10) = {0}\n",
		(
			fibonacci(10),
		)
	);
};
