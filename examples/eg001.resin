fibonacci (n: int) -> int = {
    if (n <= 1) {
        n
    } else {
        f0 = fibonacci(n - 1);
        f1 = fibonacci(n - 2);
        f0 + f1
    }
};
print("fibonacci(10) = {0}\n", (fibonacci(10),));
