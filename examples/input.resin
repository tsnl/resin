export { main };

import { "$/console.resin" };

def main() -> Result<(), _> = {
	print("What is your name? ");
	var name = Console.read_line()?;

	print("Hello, ");
	Console.print(name)?;
	print("!\n");
	ok(())
};
