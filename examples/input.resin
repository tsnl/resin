export { main };

import { "std/console.resin" };

def main() -> Result<(), _> = {
	print("What is your name? ", ());
	var name = input()?;

	print("Hello, ", ());
	print_input(name)?;
	print("!\n", ());
	ok(())
};
