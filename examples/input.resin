export { main };

import { "$/console.resin", "$/string.resin" };

def main() -> (() | Err<_>) = {
	print("What is your name? ");
	var name = Console.read_line()?;

	print("Hello, ");
	Console.print(name)?;
	print("!\n");
	(())
};
