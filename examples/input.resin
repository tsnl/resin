export { main };

import { "std/console.resin" };

def main() -> Result<(), _> = {
	print("What is your name? ", ());
	var name = Console.read_line()?;

	print("Hello, ", ());
	name.print()?;
	print("!\n", ());
	ok(())
};
