export { main };

import { "$/console.resin", "$/string.resin" };

fn main() -> (() | Err<_>) {
	print("What is your name? ");
	let mut name = console_read_line()?;

	print("Hello, ");
	console_print(name)?;
	print("!\n");
	(())
}
