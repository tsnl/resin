//! MNIST MLP training (Python `demo_mnist` analogue).
//!
//! TODO: Re-enable when `resin-jit` provides `Session`, `compile_open`, and GPU
//! minibatch writers. Dataset loading via `resin_util::dataset` is ready.

fn main() {
    panic!("TODO: resin-jit not implemented — train_mnist needs GPU session and training loop");
}
