//! E2E: compose a simple const graph and interpret it on the GPU.
//!
//! TODO: Re-enable when `resin-jit` provides `compile_open` and `Pipeline::call`.

fn main() {
    panic!("TODO: resin-jit not implemented — demo_interp needs GPU compile and call");
}
