//! Smoke: const and param paths via `PipelineFactory`.
//!
//! TODO: Re-enable when `resin-jit` provides `compile_open` and `Pipeline::call`.

fn main() {
    panic!("TODO: resin-jit not implemented — interp_add needs GPU compile and call");
}
