[package]
name = "resin-protocol"
version.workspace = true
edition = "2024"
publish = false
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
