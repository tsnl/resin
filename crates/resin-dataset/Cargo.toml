[package]
name = "resin-dataset"
version = "0.1.0"
edition = "2021"

[dependencies]
resin-core = { path = "../resin-core" }
resin-tree = { path = "../resin-tree" }
resin-tree-derive = { path = "../resin-tree-derive" }