# `resin`

A game engine for Souls-like games. A testbed for advanced metaprogramming.

Running the Python code in this repo generates a redistributable folder containing the executable to run, assets, and more. No Python code is run in the redistributable folder. Python is exclusively used as a metaprogramming language.

```bash
uv run resin-build
```
